#!/bin/bash

export __dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export __dir=$( dirname ${__dir} )

set -x

rm -i ${__dir}/src/log/*.log
rm -i ${__dir}/src/config.yml
rm -i ${__dir}/src/ipt.db

exit 0
